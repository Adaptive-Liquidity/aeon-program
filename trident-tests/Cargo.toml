[workspace]

[package]
name = "fuzz_tests"
version = "0.1.0"
description = "AEON P2 Trident fuzz — remaining_accounts / cascade / authority hierarchy"
edition = "2021"

[dependencies]
borsh = "1.5.3"

[dependencies.trident-fuzz]
version = "0.12.0"
features = ["token"]

[[bin]]
name = "remaining_accounts_p2"
path = "remaining_accounts_p2/test_fuzz.rs"
